import React from 'react';
interface Props {
    children: React.ReactNode;
    onClose?: () => void;
}
declare const MasterCard: ({ children, onClose }: Props) => JSX.Element;
export default MasterCard;
